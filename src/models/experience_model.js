export default class ExperienceEntry {
    constructor(title, description, dates,highlighted) {
        this.title = title;
        this.description = description;
        this.dates = dates;
        this.highlighted = highlighted;
    }
}
